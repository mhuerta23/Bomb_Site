
<!-- Carousel start -->
  <div id="myCarousel" class="carousel slide" data-ride="carousel">
    <!-- Indicators -->
    <ol class="carousel-indicators">
      <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
      <li data-target="#myCarousel" data-slide-to="1"></li>
      <li data-target="#myCarousel" data-slide-to="2"></li>
      <li data-target="#myCarousel" data-slide-to="3"></li>
    </ol>

    <!-- Wrapper for slides -->
    <div class="carousel-inner" role="listbox">
      <div class="item active">
        <%= image_tag('buncha-colors.jpg') %>
        <div class="carousel-caption">
            <h2>20+ Murals</h2>
        </div>
      </div>

      <div class="item">
        <%= image_tag('pugs.jpg') %>
        <div class="carousel-caption">
            <h2>Crowdsourced</h2>
        </div>
      </div>

      <div class="item">
        <%= image_tag('goblin-aliens.jpg') %>
        <div class="carousel-caption">
            <h2>Updated instantly</h2>
        </div>
      </div>

      <div class="item">
        <%= image_tag('political-figure.jpg') %>
        <div class="carousel-caption">
            <h2>Trends</h2>
        </div>
      </div>

    </div>

    <!-- Left and right controls -->
    <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
      <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
      <span class="sr-only">Previous</span>
    </a>
    <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
      <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
      <span class="sr-only">Next</span>
    </a>
  </div>
  <!-- Carousel end -->

  <div class="search">
    <h1 class="text-center">What's that mural?</h1>
    <%= form_tag("/pictures", method: "get", style: "text-align:center", placeholder: '(That describes the mural)') do %>
      <%= label_tag(:q, "Enter a word:") %>
      <%= text_field_tag(:q) %>
      <%= submit_tag("Search") %>
      <% end %>


  <hr style="weight: 350; margin: 5px">
  </div>
  <div class="bottom-panel" style="margin: 10px auto">
    <div class="row fp-row" style="">
    <% @pictures.each do |picture| %>
      <div class="col-lg-3 fp-image" style="margin: 10px auto">
        <%= link_to image_tag(picture.url, :width => 300, :height => 260), picture, :class => 'nil' %>
      </div>
    <% end %>
    </div>

  </div>

<!--
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Url</th>
      <th>Description</th>
      <th>Tag</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pictures.each do |picture| %>
      <tr>
        <td><%= picture.name %></td>
        <td><%= picture.url %></td>
        <td><%= picture.description %></td>
        <td><%= picture.tag %></td>
        <td><%= link_to 'Show', picture %></td>
        <td><%= link_to 'Edit', edit_picture_path(picture) %></td>
        <td><%= link_to 'Destroy', picture, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->

<!-- <br>

<%= link_to 'New Picture', new_picture_path %> -->
